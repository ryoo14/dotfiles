" color Scheme
colorscheme hybrid
syntax on

if has ('mac')
  set guifont=Ricty:h16
endif

if has ('unix')
  set guifont=Ricty\ 12
endif

" options
set guioptions-=m
set guioptions-=T
set lines=90 columns=200  " window size

