" color Scheme
"colorscheme hybrid

if has ('mac')
  set guifont=Ricty:h16
elseif has ('unix')
  set guifont=Ricty\ 11
endif

" options
set guioptions-=m
set guioptions-=T
set lines=90 columns=200  " window size
set notitle
