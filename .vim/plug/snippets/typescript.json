{
	"itigyo": {
		"prefix": "itigyo",
		"body": [
			"import * as fs from \"fs\";",
			"",
			"const input: string[] = fs.readFileSync(\"/dev/stdin\", \"utf8\").trim();"
		],
		"description": "atcoder snipet",
    "scope": "source.typescript"
	},
	"nigyo": {
		"prefix": "nigyo",
		"body": [
			"import * as fs from \"fs\";",
			"",
			"const input: string[] = fs.readFileSync(\"/dev/stdin\", \"utf8\").split(\"\\n\");"
		],
		"description": "atcoder snipet",
    "scope": "source.typescript"
	},
	"numarray": {
		"prefix": "numarray",
		"body": [
			"import * as fs from \"fs\";",
			"",
			"const N: number = fs.readFileSync(\"/dev/stdin\", \"utf8\").trim();",
			"const A: number[] = fs.readFileSync(\"/dev/stdin\", \"utf8\").trim().map(n => Number(n));"
		],
		"description": "atcoder snipet",
    "scope": "source.typescript"
	},
	"cl": {
		"prefix": "cl",
		"body": [
			"console.log($1);$0"
		],
		"description": "console log"
	}
}
